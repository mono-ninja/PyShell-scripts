# Tech Stack — site stack fingerprinting
# Python 3.11+ dependencies
#
# PyShell installs these via `uv pip install -r requirements.txt`.

requests>=2.28
pyyaml>=6.0

# For --render: js_globals (the most reliable version source) and real
# network requests through a headless Chromium.
#
# The cost is ~134 MB in the script's venv: that is the package itself
# with its bundled Node driver, not a browser. Playwright's browsers
# live NOT in the venv but in a shared machine-wide cache —
# ~/Library/Caches/ms-playwright (macOS) — common to every venv on the
# machine. So if any Playwright project has ever run here,
# `playwright install chromium` is not needed a second time: the
# browser is already downloaded.
#
# When the browser is missing, Tech Stack does not crash: it puts the
# `playwright install chromium` command into a warning and continues
# without rendering (see render.py).
# playwright>=1.40
